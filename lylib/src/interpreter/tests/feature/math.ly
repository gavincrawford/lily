let complex_a = 1 + 1 - 1
let complex_b = 5 * 2 / 2 ^ 2
let complex_c = (3*2)/(2-(3-2))

let floor_basic = 7 // 2
let floor_exact = 6 // 2
let floor_negative = -7 // 2

let power_basic = 2 ^ 3
let power_zero = 5 ^ 0
let power_one = 5 ^ 1
let power_fraction = 4 ^ 0.5

let zero_addition = 0 + 5
let zero_subtraction = 10 - 0
let zero_multiplication = 7 * 0
let one_multiplication = 8 * 1
let negative_addition = -5 + 3
let negative_subtraction = -2 - 4
let negative_multiplication = -3 * 4
let negative_division = -12 / 3
let large_number = 999999 + 1
let decimal_precision = 0.1 + 0.2
let mixed_operations = 2.5 * 4 - 1.5 / 3

let nested_power = 2 ^ 2 ^ 2
let floor_with_decimals = 3.7 // 1.2
let power_negative_base = -2 ^ 2
let multiple_negatives = -3 * -2
let large_overflow = 999999999999999999 * 999999999999999999
