let n_eq = 1 == 1
let n_neq = 1 != 2
let n_add = 1 + 1
let n_sub = 1 - 1
let n_mul = 4 * 2
let n_div = 16 / 2

let b_eq = false == false
let b_neq = false != true

let s_eq = "abc" == "abc"
let s_neq = "abc" != "cba"

let c_eq = 'a' == 'a'
let c_neq = 'a' != 'b'
